# Project Conversational Rules

- 学生端 AI 对话模块当前迭代优先：界面样式提升、来源引用可点击闭环、快捷问题一键发送。
- 来源引用实现策略固定为“混合方案”：优先跳知识详情页；失败时降级弹层展示摘要；若存在外链则保留外链兜底。
- 根据回复内容智能推荐相关链接/入口当前暂缓，不在本轮实现范围内。
- 当前阶段 UI 不作为阻塞项；优先实施 AI 强约束防幻觉（检索门控 + 证据不足拒答）。
- 知识库扩量优先补齐生活化高频问题（如电费、报修、水电、校园卡、网络），并可通过批量提示词并行清洗。
- 在知识库修复阶段先处理编号冲突/重复/缺源等遗留问题；并行子代理提示词需采用“单任务 + 固定输出 + 先计划后执行”的强约束模板。
- 知识库编号重映射正式执行前，必须先按草稿 frontmatter 的 `material_id` 校验内容归属；禁止仅凭旧 KB 编号直接重命名。

# 开发环境本地配置（覆盖 .env.development）
# 使用本机 IP 地址，允许其他设备访问

# API 基础地址 - 使用局域网 IP
VITE_API_BASE_URL=http://192.168.100.209:8080

# WebSocket 地址（通常与 API 同主机，自动推断）
VITE_WS_BASE_URL=ws://192.168.100.209:8080

# 应用标题
VITE_APP_TITLE=学术智治系统

# 是否开启 Mock
VITE_USE_MOCK=false

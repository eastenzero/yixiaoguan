# P0-REX-0014 复核问询包（第二课堂录入操作流程）

## 背景

- 任务ID：`P0-REX-0014`
- 材料：`MAT-20260323-0014`
- 当前状态：`extracted` + `manual_review`
- 未进入 `ready` 与 `entry draft` 正式链路

## 需业务确认项（一次性问询）

1. 该流程（招行APP激活+扫码激活+报名/取消报名+签到码扫码签到）是否仍为当前学年唯一标准流程？
2. 学生端入口是否仍为“招商银行APP-第二课堂”，是否新增/替换为其他入口（如小程序、统一门户）？
3. 管理员端“考勤管理扫码签到”页面名称与路径是否有变更？
4. “活动状态为报名中可立即报名”规则是否仍有效，是否存在新的状态机（如候补、审核后报名）？
5. 学号/姓名/院校不匹配报错提示文案是否变化，是否新增其他高频异常？
6. 是否存在新的时间限制（例如激活有效期、报名截止逻辑、补签规则）？

## 建议回收格式

- 确认结论：`保持不变 / 部分更新 / 全量更新`
- 若更新：提供
  - 新入口名称与路径
  - 新页面按钮名称
  - 新时效规则
  - 新异常处理规则
  - 生效时间

## 回写动作（确认后执行）

- 更新：`first-batch-rule-extraction-master-with-review.csv` 对应任务复核结论
- 如通过：补入 `first-batch-rule-extraction-ready.csv`
- 生成条目：新增 `KB-20260324-XXXX.md` 并加入填充清单

# 首批清洗继续执行提示词

你现在继续接手“医小管”知识库项目的首批源数据清洗工作。前一轮清洗已经完成了首轮落地，但还需要做收口和修补。你的任务不是从头开始，而是在现有产物基础上继续推进，并修正当前发现的问题。

## 1. 你接手前必须先理解的现状

当前首批样本范围已经锁定并已完成首轮处理，整体状态是：

- 首批样本共 22 份
- 材料总表补全已完成
- 清洗标注已完成
- 候选知识条目清单已完成
- 格式转换已完成大部分
- 当前转换结果为：20 成功，2 失败
- 失败的 2 份材料均为 `.doc`
- 7 份 PDF 已通过 `MinerU API` 成功产出完整结果

现有产物位置：

- 原始首批索引：
  - `knowledge-base/raw/first-batch-material-index.csv`
- 清洗后总表：
  - `knowledge-base/raw/first-batch-processing/manifests/first-batch-material-index.cleaned.csv`
- 清洗标注结果：
  - `knowledge-base/raw/first-batch-processing/manifests/first-batch-cleaning-annotations.csv`
- 候选知识条目清单：
  - `knowledge-base/raw/first-batch-processing/manifests/first-batch-candidate-knowledge-list.csv`
- 转换日志：
  - `knowledge-base/raw/first-batch-processing/logs/conversion-log.csv`
- 转换阻塞清单：
  - `knowledge-base/raw/first-batch-processing/manifests/first-batch-conversion-blockers.csv`
- 转换产物目录：
  - `knowledge-base/raw/first-batch-processing/converted/`

## 2. 当前定版口径

你必须继续遵守以下口径，不要擅自改流程：

- `knowledge-base` 是知识条目层，不是原始材料层
- 原始材料保留在 `数据库部分材料/`，不要移动或重命名原件
- 当前工作对象优先是首批 22 份样本，不要先发散到全仓
- 固定顺序仍然是：
  1. 总表维护
  2. 格式转换
  3. 清洗标注
  4. 候选条目整理
- 格式转换口径固定：
  - `md`：保留 UTF-8
  - `doc/docx -> Markdown`
  - `xls/xlsx -> CSV`
  - `pdf -> MinerU API`
- `pdf` 不再依赖桌面版 MinerU 手工操作

## 3. 你必须先阅读这些文件

1. `docs/08-源数据清洗标准.md`
2. `docs/09-材料总表字段规范.md`
3. `docs/10-首批高价值材料清单.md`
4. `docs/11-格式转换执行方案.md`
5. `docs/12-MinerU API与格式转换交接说明.md`
6. `docs/13-首批清洗继续执行提示词.md`
7. `knowledge-base/raw/first-batch-material-index.csv`
8. `knowledge-base/raw/first-batch-processing/manifests/first-batch-material-index.cleaned.csv`
9. `knowledge-base/raw/first-batch-processing/manifests/first-batch-candidate-knowledge-list.csv`
10. `knowledge-base/raw/first-batch-processing/manifests/first-batch-conversion-blockers.csv`

## 4. 当前已确认的问题

你接手后优先处理以下问题：

### 问题 A：`primary_reference` 字段契约未统一

现状：

- 实际清洗产物里，`primary_reference` 已经存的是主参考 `material_id`
- 但 `docs/09-材料总表字段规范.md` 里还写的是“是否主参考文件 / 是或否”

你的处理要求：

- 把文档口径改成和实际产物一致
- `primary_reference` 统一定义为“主参考 `material_id`”
- 不再使用布尔值语义

### 问题 B：`学生手册` 候选问题粒度过大

现状：

- 候选清单里有一条问题是：
  - “学生手册中与学工相关的核心办事规则有哪些？”
- 这条问题过大，而且分类仍是 `待定`

你的处理要求：

- 不允许把它作为单条最终候选问题继续往下传
- 必须把 `学生手册` 按主题拆成多个更小的候选问题
- 每条问题都要落到明确分类，例如：
  - 入学与学籍
  - 奖助贷补
  - 心理与测评
  - 证件与校园服务
  - 竞赛与第二课堂
- 拆分后替换原有这条过大的候选项

### 问题 C：2 份 `.doc` 转换失败，仍需补齐

阻塞材料：

- `MAT-20260323-0002`
- `MAT-20260323-0003`

已知错误：

- `doc -> Word COM -> docx` 阶段失败
- 错误类型：`COMException`
- 现有替代方案已写入阻塞清单：
  - 复制到 ASCII 临时路径
  - 再用 Word COM `SaveAs2` 转 `docx`
  - 再用 `pandoc` 转 `md`

你的处理要求：

- 优先重试这两份 `.doc`
- 如果重试成功，更新：
  - 清洗后总表
  - 转换日志
  - 阻塞清单
- 如果仍失败，必须明确失败原因和下一步替代方案，不能只保留“failed”

### 问题 D：字段命名不统一

现状：

- 清洗总表使用 `conversion_status`
- 转换日志使用 `status`

你的处理要求：

- 评估是否要统一命名
- 如统一，优先保持全项目一致
- 但不要为了格式美观破坏已有可用产物

## 5. 你的下一步执行顺序

请严格按以下顺序继续：

1. 先修正文档中 `primary_reference` 的定义
2. 再拆分 `学生手册` 的候选问题
3. 再重试 `MAT-20260323-0002` 和 `MAT-20260323-0003` 的 `.doc` 转换
4. 再回写所有受影响的清单和日志
5. 最后给出“首批 22 份样本是否可进入规则抽取阶段”的明确结论

## 6. 你的输出要求

你至少需要交付：

1. 修订后的 `docs/09-材料总表字段规范.md`
2. 更新后的候选知识条目清单
3. 更新后的清洗后总表
4. 更新后的转换日志
5. 更新后的阻塞清单
6. 一段简短总结，明确回答：
   - 首批样本还有哪些未完成项
   - 是否已具备进入“规则抽取草案表”阶段的条件

## 7. 工作边界

- 不要把工作重新扩展到全仓
- 不要跳过收口问题直接大批量写知识正文
- 不要改动原始材料目录结构
- 不要把“模板类材料”直接当成最终 FAQ 正文

## 8. 开始执行前的回复格式

你开始前先回复三件事：

1. 你理解的当前首批进度状态
2. 你将优先处理的 4 个问题
3. 你准备修改或更新的具体文件清单

然后再开始执行。

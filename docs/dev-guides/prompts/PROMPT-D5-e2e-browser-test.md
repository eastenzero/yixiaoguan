# 【提示词 D5】学生端端到端真机验证（浏览器操控版）

> **状态**：待执行 | **预估工时**：30~60分钟  
> **前置条件**：TASK-D1~D4 均已完成；所有后端服务需已启动  
> **特别说明**：本提示词需要使用你的**浏览器操控能力**（Browser Agent / Computer Use）进行真实的 UI 交互测试

---

## 提示词正文（直接粘贴给支持浏览器操控的 AI）

---

你是 **医小管（yixiaoguan）** 项目的 **QA 测试工程师**。你拥有**直接使用浏览器**的能力。

本次任务是对学生端 H5 应用进行**端到端真实浏览器验证**。

**【前置条件确认——开始测试前先核查】**

在开始浏览器操作前，先验证各服务是否已就绪：

```powershell
# 检查端口是否在监听（PowerShell）
Test-NetConnection -ComputerName localhost -Port 8080  # business-api
Test-NetConnection -ComputerName localhost -Port 8000  # ai-service
Test-NetConnection -ComputerName localhost -Port 5174  # student-app H5
```

如果某个端口未监听，根据 `.globalrules` 中的"🚀 服务启动方式"章节逐一启动对应服务。
**如果 business-api（8080 端口）未能成功启动，请先停下来报告错误信息，不要继续进行浏览器测试。**

---

**【测试账号】**
- 学号（用户名）：`4523570155`
- 密码：`admin123`

---

**【测试场景一：登录流程】**

1. 使用浏览器访问 `http://localhost:5174`
2. **截图**：记录登录页面的实际样式（确认验证码是否正常显示）
3. 在学号输入框输入 `4523570155`，密码输入框输入 `admin123`
4. 如果有验证码输入框，先刷新验证码并输入
5. 点击登录按钮

**预期结果**：跳转至首页，顶部显示欢迎语（含真实姓名）

**如果登录失败**，请：
- 截图记录错误弹窗或网络请求的报错信息（打开 DevTools → Network 标签）
- 记录失败的接口路径、HTTP 状态码、响应 body
- 不要继续后续测试，直接在报告中记录详细错误并停止

---

**【测试场景二：首页功能入口】**

登录成功后：
1. **截图**：记录首页完整样式
2. 确认 4 个功能入口卡片（AI 咨询、申请、进度、知识问答）是否可见
3. 点击每个入口，确认跳转正确（可以用后退键返回）

**预期结果**：4 个入口均可点击，跳转到对应页面（即使页面是占位也算通过）

---

**【测试场景三：AI 对话（核心场景）】**

1. 进入 AI 咨询页（`/pages/chat/index`）
2. **截图**：记录 AI 对话页初始状态
3. 在输入框输入：`"学校图书馆几点开门？"` 并点击发送
4. 等待 AI 响应（最长等待 30 秒）
5. **截图**：记录 AI 回复后的消息气泡状态

**验证点**：
- 学生消息气泡是否出现（右侧绿色）
- AI 回复是否出现（左侧白色）
- **AI 回复是否是真实内容**（而非 mock 固定文字"您好，我是医小管AI助手..."）
- 打开 DevTools → Network，确认是否有 `/api/chat` 的请求，记录请求状态码

**如果 AI 回复是错误提示"AI 服务暂时不可用"**：
- 记录 `/api/chat` 请求的实际 HTTP 状态码和响应 body
- 在报告中标注 ai-service 是否正常响应

---

**【测试场景四：空教室申请】**

1. 进入空教室申请页（`/pages/apply/classroom`）
2. **截图**：记录表单初始状态（重点：教室下拉列表是否已加载）
3. 打开 DevTools → Network，查看 `GET /api/v1/classrooms` 的请求结果
4. 如果教室列表为空：记录接口响应数据
5. 尝试填写表单（随意选择一个教室、选明天的日期、选 10:00~12:00、填"测试申请"为用途）并点击提交
6. **截图**：提交后的结果（成功跳转 / 报错弹窗）

**验证点**：
- 教室下拉列表是否有真实数据
- 提交后返回的 HTTP 状态码

---

**【测试场景五：我的申请列表】**

1. 进入我的申请页（`/pages/apply/status`）
2. **截图**：记录申请列表（有数据 / 空状态）
3. 打开 DevTools → Network，查看 `GET /api/v1/classroom-applications` 的请求
4. 记录请求是否携带了 `applicantId` 参数

---

**【你的交付物】**

1. 每个测试场景的**截图**（共 5~8 张）
2. 一份详细的验证报告，写入：
   `docs/test-reports/completion-reports/TASK-D5-e2e-browser-report.md`

---

**【报告格式要求】**

报告必须包含：

```markdown
# TASK-D5 学生端端到端浏览器验证报告

## 任务标识
- 执行时间：YYYY-MM-DD HH:mm:ss（精确到秒）
- 执行人：AI Agent (Browser)
- 测试环境：http://localhost:5174

## 服务状态
| 服务 | 端口 | 状态 |
|------|------|------|
| student-app | 5174 | ✅/❌ |
| business-api | 8080 | ✅/❌ |
| ai-service   | 8000 | ✅/❌ |

## 测试场景结果
| 场景 | 操作 | 预期 | 实际 | 状态 |
|------|------|------|------|------|
| 场景一：登录 | ... | ... | ... | ✅/❌ |
| 场景二：首页 | ... | ... | ... | ✅/❌ |
| 场景三：AI对话 | ... | ... | ... | ✅/❌ |
| 场景四：申请表单 | ... | ... | ... | ✅/❌ |
| 场景五：申请列表 | ... | ... | ... | ✅/❌ |

## 发现的 Bug 或接口问题
（每个问题：现象描述 + 接口路径 + HTTP 状态码 + 响应 body 摘要）

## 截图清单
（每张截图的路径 + 说明）

## 结论与下一步建议
```

---

**【禁止清单】**
- ❌ 禁止修改任何项目文件（纯验证，不写代码）
- ❌ 禁止因为某一场景失败就跳过后续场景——每个场景独立记录，能测多少测多少
- ❌ 禁止对测试结果进行"乐观估计"——测试失败就如实写 ❌，不要美化

**现在，请先执行服务端口检测，确认所有服务正常后开启浏览器，从场景一开始测试。**

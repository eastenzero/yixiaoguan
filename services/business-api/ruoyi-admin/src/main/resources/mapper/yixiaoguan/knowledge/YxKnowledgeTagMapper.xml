<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yixiaoguan.knowledge.mapper.YxKnowledgeTagMapper">

    <resultMap id="BaseResultMap" type="com.yixiaoguan.knowledge.domain.YxKnowledgeTag">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="isDeleted" column="is_deleted"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, name, sort_order, created_at, updated_at, is_deleted
    </sql>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM yx_knowledge_tag
        WHERE is_deleted = FALSE
        ORDER BY sort_order ASC, id ASC
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM yx_knowledge_tag
        WHERE id = #{id} AND is_deleted = FALSE
    </select>

    <select id="selectByName" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM yx_knowledge_tag
        WHERE name = #{name} AND is_deleted = FALSE
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO yx_knowledge_tag (name, sort_order)
        VALUES (#{name}, #{sortOrder})
    </insert>

    <update id="update">
        UPDATE yx_knowledge_tag
        SET name = #{name},
            sort_order = #{sortOrder},
            updated_at = NOW()
        WHERE id = #{id} AND is_deleted = FALSE
    </update>

    <update id="deleteById">
        UPDATE yx_knowledge_tag
        SET is_deleted = TRUE, updated_at = NOW()
        WHERE id = #{id} AND is_deleted = FALSE
    </update>

</mapper>
